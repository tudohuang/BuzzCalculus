# 行尾一律不轉換。
#
# 這個 repo 在 Windows 上開發（core.autocrlf=true 是機器預設），
# 但檔案實際上全是 LF —— 於是每次 commit 都噴一整頁
# "LF will be replaced by CRLF" 警告。宣告 -text 讓 git 完全不碰行尾：
# 檔案是什麼樣就存什麼樣，零轉換、零警告、零 renormalize churn。
* -text
