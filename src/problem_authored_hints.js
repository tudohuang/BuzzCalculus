(function () {
  "use strict";

  // 補提示的側表（2026-08）。
  //
  // 為什麼要側表：這些題散在六個題庫檔裡，而那六個檔的結構各不相同
  // （有 IIFE、有純陣列、有各自的 helper）。為了補兩行提示去改六種結構，
  // 出錯的機會遠大於收益。側表只做一件事：照 id 把提示併回題目物件。
  //
  // 併回**物件**而不是另外開一條讀取路徑，是因為 app.js 的 authoredHints
  // 與 tools/validate_hints.js 都直接讀 problem.hints ——
  // 多開一條路徑就要同時改兩邊，而且從此有兩個地方可能不一致。
  //
  // 只補「原本一條提示都沒有」的題，不覆蓋作者已經寫好的。
  //
  // 寫法的規矩（跟各題包裡的一致）：
  //   第一層說「該看出什麼」，第二層說「關鍵那一步」，兩層都不說出答案。
  //   不要寫「先判型」「認出主要工具」這種對每一題都成立、也就是對每一題
  //   都沒用的句子 —— tools/validate_hints.js 會把它們統計成罐頭。
  const HINTS = {
    /* ── 國際難題包 ── */
    "world-019": [
      "log x 在分母，直接找不到反導數。把 x² 看成 x^a 的特例，改成一族積分來處理。",
      "令 I(a) = ∫₀¹(x^a−1)/log x dx，對 a 微分之後 log x 會被消掉，剩下的積分非常簡單。"
    ],
    "world-023": [
      "先做部分分式。1/(n(n+1)) 本身可以拆，而它的平方展開後會出現三種型別的項。",
      "拆完會湊出一個 ζ(2)、一個望遠鏡和、以及一個常數。三塊分開算再合起來。"
    ],
    "world-034": [
      "調和數的生成函數是 −log(1−x)/(1−x)。這題比它多了一個 1/n，代表要再積一次。",
      "寫成 ∫₀^{1/2} 的積分之後，被積函數會出現 log(1−x)/x 的形狀。"
    ],
    "world-037": [
      "用參數微分：把 cos 裡的係數換成 t，令 I(t) = ∫₀^∞ e^{−x²}cos(tx)dx。",
      "對 t 微分之後分部積分，會得到 I′ = −(t/2)·I 這個一階 ODE。初值就是高斯積分。"
    ],
    "world-044": [
      "分部積分：把 cot x 積掉會得到 log(sin x)。",
      "剩下的是 −∫₀^{π/2}log(sin x)dx，那是一個有標準結果的經典積分。"
    ],
    "world-051": [
      "這是有名字的積分（Ahmed 積分）。硬算沒有出路，要靠參數化。",
      "把 arctan 寫成一個對參數 t 的積分，交換積分次序之後兩層都算得動。"
    ],
    "world-052": [
      "用 Beta 函數的參數微分：∫₀^{π/2}sin^s x dx 對 s 微分兩次，就把 log² 拉下來了。",
      "在 s=0 取值時會用到 Γ 函數的一階與二階對數導數（ψ 與 ψ′），結果會同時含 log²2 與 π³。"
    ],
    "world-054": [
      "先想一步：在 [0, π/2] 上，代換 x → π/2 − x 會把 cos 換成 sin。",
      "所以它跟 log²(sin x) 那一題同值。真正要算的是 Beta 函數對指數的二階導數。"
    ],
    "world-058": [
      "跟 cos(2x) 那一題同一個手法：把 cos 裡的係數換成參數 t 做參數微分。",
      "對 t 微分再分部積分會得到一個一階 ODE；解它、用高斯積分定初值，最後把 t 代回 1。"
    ],
    "world-070": [
      "sin x / x 沒有初等原函數。引進參數：把 e^{−x} 換成 e^{−sx}。",
      "對 s 微分會把分母的 x 消掉，剩下一個標準的 ∫e^{−sx}sin x dx；積回去會出現 arctan，再代 s=1。"
    ],
    "world-074": [
      "中央二項係數有一個標準的漸近式，來源是 Stirling 公式。",
      "把 C(2n,n) 用 Stirling 展開，4^n 會跟分子抵消，剩下 √n 與一個常數。"
    ],
    "world-075": [
      "部分分式：1/(n²(n+1)) 會拆成三項。",
      "其中兩項會望遠鏡相消，只留下一個 ζ(2) 與一個常數。"
    ],
    "world-097": [
      "調和數的生成函數是 Σ H_n x^n = −log(1−x)/(1−x)。",
      "這題不必再積分，直接代 x = 1/2 就結束了。"
    ],
    "putnam-003": [
      "先取對數：會變成 (1/n)·Σ log(1+k/n) —— 這是黎曼和的長相。",
      "極限是 ∫₀¹log(1+x)dx，用分部積分算完之後記得取指數回去。"
    ],

    /* ── 高速反射包（東大題型）── */
    "td-lim-002": [
      "先取對數：log Pₙ = Σ log(1+k/n²)。把 log(1+t) 展開到 t² 那一項。",
      "展開後 log Pₙ 會長成「常數 + c/n + O(1/n²)」。指數回去再減，乘上 n 之後只剩 c 乘那個指數。"
    ],
    "td-int-003": [
      "兩個指數相減、再除以 x，而且積到無窮 —— 這是 Frullani 積分的標準長相。",
      "Frullani 給的結果只跟兩個係數的比值有關。先確認哪一個係數在分子。"
    ],
    "td-int-006": [
      "被積函數是偶函數，先化成 2∫₀^∞。接著用高斯積分對參數微分。",
      "令 I(a) = ∫e^{−ax²}dx = √(π/a)，對 a 微分兩次會把 x⁴ 拉下來（兩個負號相消），再代入係數。"
    ],
    "td-ser-003": [
      "雙階乘的比值可以用 Wallis 積分表示：(2n−1)!!/(2n)!! 正比於 ∫₀^{π/2}sin^{2n}x dx。",
      "代進去之後交換求和與積分的次序，Σxⁿ/n 會併成 −log(1−x)。"
    ],
    "td-int-002": [
      "這是一族積分 ∫₀^π log(a + b cos x)dx。對參數 a 微分。",
      "微分後是有理三角積分，用 Weierstrass 代換算得出來；再對 a 積回去，用 b=0 的情形定積分常數。"
    ],
    "td-int-005": [
      "那個不等式圍出來的是一個橢圓。把二次型寫成對稱矩陣，面積只跟它的行列式有關。",
      "做線性變數變換把它化成單位圓：Jacobian 是 1/√(det A)，而單位圓的面積是 π。"
    ],

    /* ── 手機進階包：問的是「用哪個技巧」，不是問值 ── */
    "mob-tech-011": [
      "先看形狀：同一個函數的兩個縮放版本相減，再除以 x，而且積到無窮 —— 這個組合有專屬的定理。",
      "不要去找原函數。想一想哪一個定理是專門處理這種「兩個指數相減再除以 x」的積分。"
    ],
    "mob-tech-018": [
      "看係數的排列：x² 配二階導、x 配一階導，再加上 (x² − ν²)。這是一個有名字的方程。",
      "少了那個 x² 項就是 Euler–Cauchy 方程；多了它之後，它變成另一族特殊函數的定義方程。"
    ],

    /* ── 競賽包 ── */
    "hd-005": [
      "不要真的微分四次。1/(1+x²) 是一個幾何級數的和。",
      "泰勒係數與導數的關係是 f⁽ⁿ⁾(0) = n!·aₙ。找出 x⁴ 的係數，再乘上 4!。"
    ],
    "app-012": [
      "先寫出曲率公式 κ = |y″|/(1+y′²)^{3/2}，把 y′ 與 y″ 代進去。",
      "化簡後 κ 是 x 的函數，對 x 微分設 0。注意定義域只有 x>0，端點不用檢查。"
    ],

    /* ── 核心擴充包 ── */
    "cx-hi-008": [
      "分子是兩個指數相減、分母是 x。這個形狀不必求原函數就有結果。",
      "答案只跟兩個指數的係數比有關 —— 先確認比值裡哪一個在上面。"
    ],
    "cx-hs-006": [
      "先寫出調和數的生成函數。它可以從 Σxⁿ 與 1/(1−x) 的堆疊看出來。",
      "生成函數是 −log(1−x)/(1−x)，把 x = 1/2 代進去就結束了。"
    ],

    /* ── 應用與圖形包 ── */
    "ap-rr-005": [
      "先把仰角寫成高度的函數：θ = arctan(y/40)。",
      "對時間微分，arctan 的導數分母是 1 + (y/40)²；別忘了鏈鎖律要乘上高度的變化率。"
    ],
    "gp-001": [
      "g(4) 就是 f 從 0 到 4 圍出來的面積 —— 直接看圖，不必找 f 的式子。",
      "圖上那塊是一個三角形，用底乘高除以 2。"
    ],
    "gp-003": [
      "g″ = f′，所以 g 的凹向是由 f 的**斜率**決定的，不是由 f 的正負。",
      "找 f 從上升轉為下降的那個 x —— 斜率在那裡變號。"
    ],
    "gp-004": [
      "位移是速度的定積分，所以 x 軸下方的部分要算成負的。",
      "把上方與下方的面積分別算出來再相減。"
    ],
    "gp-005": [
      "路程積的是速率，也就是 |v| —— 下方的部分要取絕對值變成正的。",
      "把每一段三角形的面積都算成正值再相加。"
    ],
    "gp-011": [
      "g′ = f，所以 g 的最大值出現在 f 由正轉負的那一點。",
      "找到那個 x 之後，g 在那裡的值就是從 0 到該點的面積：一個矩形加一個三角形。"
    ]
  };

  const list = window.BUZZ_PROBLEMS || [];
  let applied = 0;
  list.forEach((problem) => {
    const hints = HINTS[problem.id];
    // 不覆蓋作者已經寫在題目上的提示 —— 側表只補空的
    if (!hints || (problem.hints && problem.hints.length)) return;
    problem.hints = hints.slice();
    applied += 1;
  });

  // 這個檔案必須排在所有題庫檔之後才有東西可以補。
  // 排錯位置的話它會安靜地什麼都不做，所以留一個可以檢查的痕跡。
  window.BUZZ_AUTHORED_HINTS_APPLIED = applied;
})();
