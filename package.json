{
  "name": "buzzcalculus",
  "private": true,
  "description": "零 build 的靜態站；這個檔只為了 `npm run typecheck`（TypeScript 只在開發與 CI 用，不進 runtime）。",
  "scripts": {
    "typecheck": "npx --yes -p typescript@5.6.3 tsc -p tsconfig.json"
  }
}
